# Optional local Docker Compose overrides. GitHub Actions supplies immutable
# GHCR image names and the Git commit SHA during production deployment.
WEB_IMAGE=olegthelilfix-web
IMAGE_TAG=local

# Contact address used by Caddy when requesting Let's Encrypt certificates.
ACME_EMAIL=hello@olegthelilfix.me
